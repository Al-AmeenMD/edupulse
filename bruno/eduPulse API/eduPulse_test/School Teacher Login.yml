info:
  name: School Teacher Login
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "admin@zenithacademy.com",
        "password": "Admin@12345"
      }
  auth:
    type: bearer
    token: "{{teacher_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
